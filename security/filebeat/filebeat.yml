# ============================================================
# Mini-SOC Platform - Filebeat Configuration (Filestream)
# ============================================================

filebeat.inputs:
  - type: filestream
    id: wazuh-alerts
    enabled: true
    paths:
      - /var/ossec/logs/alerts/alerts.json
    parsers:
      - ndjson:
          target: ""
          add_error_key: true
    # Forcer la lecture depuis le début du fichier (très important en CI)
    prospector.scanner.check_interval: 2s

processors:
  - add_host_metadata: ~

# ============================================================
# INDEX TEMPLATES & ILM (Désactivés pour OpenSearch)
# ============================================================
setup.template.enabled: false
setup.ilm.enabled: false
setup.template.name: "wazuh-alerts"
setup.template.pattern: "wazuh-alerts-*"

# ============================================================
# OUTPUT - OpenSearch
# ============================================================
output.elasticsearch:
  hosts: ["http://opensearch:9200"]
  # Correction : yyyy en minuscules (requis par Go/Beats)
  index: "wazuh-alerts-%{+yyyy.MM.dd}"
  allow_older_versions: true

# ============================================================
# LOGGING
# ============================================================
logging.level: info
logging.to_files: false